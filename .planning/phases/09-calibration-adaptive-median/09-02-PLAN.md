---
phase: 09-calibration-adaptive-median
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales copy/fr/ns1.json
  - src/locales copy/en/ns1.json
  - src/modules/experiment/jspsych/stimulus.ts
autonomous: true
requirements: [REQ-015, REQ-037, REQ-038]
must_haves:
  truths:
    - "Calibration Part 2 instruction screen shows GSD Phase 9 FR text about filling a bar as high as possible"
    - "FR locale key for calibration Part 2 directions matches GSD wording exactly"
    - "EN locale key mirrors the FR content in English"
    - "The video element is removed from calibrationPart2Stimuli layout"
  artifacts:
    - path: "src/locales copy/fr/ns1.json"
      provides: "Updated FR calibration Part 2 instruction text"
      contains: "remplir une barre"
    - path: "src/locales copy/en/ns1.json"
      provides: "Updated EN calibration Part 2 instruction text"
      contains: "fill a bar"
    - path: "src/modules/experiment/jspsych/stimulus.ts"
      provides: "Updated calibrationPart2Stimuli layout without video"
      contains: "calibrationPart2Stimuli"
  key_links:
    - from: "src/modules/experiment/jspsych/stimulus.ts"
      to: "src/locales copy/fr/ns1.json"
      via: "CALIBRATION_PART_2_DIRECTIONS constant reads i18n key"
      pattern: "CALIBRATION_PART_2_DIRECTIONS"
---

<objective>
Update Calibration Part 2 instruction text and screen layout to match GSD Phase 9 wording.

Purpose: The instruction screen before CalibrationPart2 trials must show the researcher-approved French text about filling a red bar as high as possible, replacing the current technical calibration wording and removing the demonstration video.

Output: Updated FR/EN locale keys, updated stimulus function layout.
</objective>

<execution_context>
@C:/Users/mijsters/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mijsters/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@src/locales copy/fr/ns1.json
@src/locales copy/en/ns1.json
@src/modules/experiment/jspsych/stimulus.ts
@src/modules/experiment/utils/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FR and EN locale keys for CalibrationPart2 directions</name>
  <read_first>
    - src/locales copy/fr/ns1.json
    - src/locales copy/en/ns1.json
  </read_first>
  <files>
    src/locales copy/fr/ns1.json,
    src/locales copy/en/ns1.json
  </files>
  <action>
1. In "src/locales copy/fr/ns1.json", find the "CALIBRATION_PART_2_DIRECTIONS" key and replace its value with GSD Phase 9 FR wording. The new value must contain these elements:
   - Opening text: "Maintenant vous allez vous entrainer a remplir une barre comme sur l'image ici."
   - Step 1: "Allez-y, maintenez la touche S enfoncee tout au long."
   - Step 2: "GO ! Tapez la touche {{TAP_KEY}} de maniere repetee le plus rapidement possible."
   - Step 3: "Le but est de faire monter la barre rouge aussi haut que possible !"
   - Use an ordered list (ol/li) for the 3 steps
   - The {{TAP_KEY}} interpolation variable must remain (it is replaced at runtime by the constants.ts function)

The exact JSON value to use:
"CALIBRATION_PART_2_DIRECTIONS": "<p style=\"text-align:left;\">Maintenant vous allez vous entra\u00eener \u00e0 remplir une barre comme sur l'image ici.</p><ol><li>Allez-y, maintenez la touche S enfonc\u00e9e tout au long.</li><li><b>GO !</b> Tapez la touche {{TAP_KEY}} de mani\u00e8re r\u00e9p\u00e9t\u00e9e <b>le plus rapidement possible</b>.</li><li>Le but est de faire monter la barre rouge aussi haut que possible !</li></ol>"

2. In "src/locales copy/en/ns1.json", replace "CALIBRATION_PART_2_DIRECTIONS" with EN mirror:
"CALIBRATION_PART_2_DIRECTIONS": "<p style=\"text-align:left;\">Now you will practice filling a bar as shown in the image here.</p><ol><li>Go ahead, hold the S key down throughout.</li><li><b>GO!</b> Tap the {{TAP_KEY}} key repeatedly <b>as fast as possible</b>.</li><li>The goal is to get the red bar as high as possible!</li></ol>"
  </action>
  <verify>
    <automated>cd "C:/Users/mijsters/Documents/LNCOai/apps/cognitive-apathy-online-patients-lnco-ai-app" && grep -c "remplir une barre" "src/locales copy/fr/ns1.json" && grep -c "fill a bar" "src/locales copy/en/ns1.json"</automated>
  </verify>
  <acceptance_criteria>
    - grep "remplir une barre" in src/locales copy/fr/ns1.json returns 1 match
    - grep "fill a bar" in src/locales copy/en/ns1.json returns 1 match
    - grep "le plus rapidement possible" in src/locales copy/fr/ns1.json returns at least 1 match
    - grep "as fast as possible" in src/locales copy/en/ns1.json returns at least 1 match
    - The {{TAP_KEY}} interpolation variable is present in both locale files within the CALIBRATION_PART_2_DIRECTIONS value
  </acceptance_criteria>
  <done>
    FR locale CALIBRATION_PART_2_DIRECTIONS contains GSD Phase 9 wording with "remplir une barre" and "le plus rapidement possible". EN locale mirrors in English. Both retain {{TAP_KEY}} interpolation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update calibrationPart2Stimuli to remove video and use text-only layout</name>
  <read_first>
    - src/modules/experiment/jspsych/stimulus.ts
    - src/modules/experiment/utils/constants.ts
  </read_first>
  <files>
    src/modules/experiment/jspsych/stimulus.ts
  </files>
  <action>
In "src/modules/experiment/jspsych/stimulus.ts", find the `calibrationPart2Stimuli` function (currently around line 463-484). It currently contains a video element inside a fieldset. Replace the entire function body with a simpler text-only layout that removes the video:

Replace the current implementation:
```
export const calibrationPart2Stimuli = (
  keySettings: ExtendedKeySettings,
): string => `
  <div style="display: flex; flex-direction: column; ...">
    <h2>${CALIBRATION_HEADER()}</h2>
    <h3>${CALIBRATION_PART()} 2</h3>
    ... (includes video fieldset) ...
  </div>
`;
```

With this new implementation (no video, clean centered layout):
```
export const calibrationPart2Stimuli = (
  keySettings: ExtendedKeySettings,
): string => `
  <div style="display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; padding: 0 20px;">
    <h2>${CALIBRATION_HEADER()}</h2>
    <h3>${CALIBRATION_PART()} 2</h3>
    <div style="max-width: 700px; text-align: left; margin: 0 auto;">
      ${CALIBRATION_PART_2_DIRECTIONS(keySettings)}
    </div>
    <div style="text-align: center; margin-top: 5%;">
      <p style="color: #333; margin: 0 auto; line-height: 1.5;">
        ${CLICK_BUTTON_TO_PROCEED_MESSAGE()}
      </p>
    </div>
  </div>
`;
```

Key changes:
- REMOVED: The entire fieldset with the video element (calibration-part2.mp4)
- REMOVED: The side-by-side flex layout that placed text next to video
- KEPT: CALIBRATION_HEADER(), CALIBRATION_PART() 2 heading, CALIBRATION_PART_2_DIRECTIONS(keySettings), CLICK_BUTTON_TO_PROCEED_MESSAGE()
- The layout is now a simple centered column, matching the pattern used by calibrationPart1Stimuli

Do NOT modify calibrationPart1Stimuli, finalCalibrationPart1Stimuli, or finalCalibrationPart2Stimuli.
  </action>
  <verify>
    <automated>cd "C:/Users/mijsters/Documents/LNCOai/apps/cognitive-apathy-online-patients-lnco-ai-app" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "video" src/modules/experiment/jspsych/stimulus.ts does NOT match inside calibrationPart2Stimuli function (the video element is removed)
    - grep "calibration-part2.mp4" src/modules/experiment/jspsych/stimulus.ts returns zero matches
    - grep "CALIBRATION_PART_2_DIRECTIONS" src/modules/experiment/jspsych/stimulus.ts still returns a match (the constant is still used)
    - grep "CALIBRATION_HEADER" src/modules/experiment/jspsych/stimulus.ts still returns matches
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    calibrationPart2Stimuli no longer contains a video element. Layout is a clean centered column with the updated GSD text from locale. All other calibration stimulus functions unchanged. TypeScript compiles.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. FR locale contains "remplir une barre" and "le plus rapidement possible" in CALIBRATION_PART_2_DIRECTIONS
3. EN locale contains "fill a bar" and "as fast as possible" in CALIBRATION_PART_2_DIRECTIONS
4. No video element remains in calibrationPart2Stimuli
5. Other calibration stimulus functions are unchanged
</verification>

<success_criteria>
- Calibration Part 2 instruction screen text matches GSD Phase 9 French wording
- EN locale mirrors FR content
- Video element removed from Part 2 instruction screen
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-calibration-adaptive-median/09-02-SUMMARY.md`
</output>
